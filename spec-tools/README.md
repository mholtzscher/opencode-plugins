# OpenCode spec tools

OpenCode V2 TUI workflow plugin.

It registers `/create-spec`, `/implement-spec`, `/implement-spec-stacked`,
`/scrub-spec`, `/simplify-spec`, `/spec-annotate`, and `/scrub-spec-bg`.
Commands that operate on existing specifications use an interactive picker for
files under the current project's `specs/` directory.
