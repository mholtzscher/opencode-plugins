# OpenCode GitHub tools

OpenCode V2 TUI workflow plugin.

It registers `/pr`, `/pr-comments`, `/pr-comments-fix`, `/pr-actions`, and
`/pr-review`. The plugin requires an authenticated `gh` CLI; `/pr-review` also
requires Plannotator.
