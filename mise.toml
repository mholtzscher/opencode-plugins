[tasks.opencode]
description = "Launch OpenCode with local plugins and an isolated development config"
run = "XDG_CONFIG_HOME={{config_root}}/.opencode-dev GIT_CONFIG_GLOBAL=$HOME/.config/git/config opencode --standalone"
